language: java
jdk:
  - oraclejdk8
install:
  - mvn install -DskipTests=true -Dmaven.javadoc.skip=true -B -V
os: linux
  dist: trusty
deploy:
  provider: heroku
  api-key:
    secure: $HEROKU_API_KEY
  app: desafioapi-travis